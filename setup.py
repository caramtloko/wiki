import os

from setuptools import setup, find_packages

setup(
    name="wiki",
    version="0.1",
    description="wiki Distribution Package",
    packages=find_packages(),
    include_package_data=True
)